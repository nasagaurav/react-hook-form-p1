import react from 'react';
import { toast } from 'react-toastify';
function View() {
  const Delete = (id) => {
    toast('deleted successfully');
  };
  return <div> </div>;
}
export default View;

import react from 'react';
import { toast } from 'react-toastify';

function Add() {
  const insert = () => {
    toast('new iten added');
  };
  return <div> Add </div>;
}
export default Add;
